# PR마다 자동으로 만들어지는 배포 프리뷰(deploy preview)를 건너뜁니다.
# 변경 사항은 로컬 서버 확인 후 PR을 올리므로 프리뷰 배포가 따로 필요 없고,
# Netlify 크레딧(프로덕션 배포 크레딧) 소비를 줄이기 위함입니다.
# main 브랜치로 머지될 때의 실제 배포(프로덕션 배포)에는 영향이 없습니다.
[context.deploy-preview]
  ignore = "exit 0"
