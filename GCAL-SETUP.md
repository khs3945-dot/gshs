# 날짜별 보기 — 구글 캘린더 연동 설정

`date.html`("날짜로 보기" 페이지)은 학교 구글 캘린더의 그날 일정도 함께 보여줍니다.
이걸 실제로 작동시키려면 Google Calendar API 키가 하나 필요합니다. (무료, 5분 정도)

## 1. API 키 발급
1. https://console.cloud.google.com 접속 → 로그인 화면 설정 때 만든 프로젝트를 그대로 사용하면 됩니다
   (없다면 `AUTH-SETUP.md`의 1번 단계를 먼저 참고해 새 프로젝트를 만드세요).
2. 왼쪽 메뉴 **API 및 서비스 → 라이브러리**에서 "Google Calendar API" 검색 → **사용 설정** 클릭.
3. **API 및 서비스 → 사용자 인증 정보 → 사용자 인증 정보 만들기 → API 키** 클릭.
4. 발급된 API 키를 복사합니다. (선택: 키를 눌러 "API 제한사항"에서 "Google Calendar API"만
   허용하도록 제한해두면 더 안전합니다.)

## 2. 사이트에 적용
`date.html` 파일 안의 아래 줄을 발급받은 키로 교체하세요.

```javascript
const GCAL_API_KEY = 'YOUR_GOOGLE_API_KEY'; // ← 여기를 교체
```

## 3. 캘린더 공개 설정 확인 (중요)
API 키만으로 조회하려면 그 캘린더가 **"공개 캘린더"**로 설정되어 있어야 합니다.
- 구글 캘린더 웹사이트 → 왼쪽에서 해당 캘린더 옆 점 3개 → **설정 및 공유**
- **액세스 권한** 항목에서 **"공개 사용 설정"**(일반 공개로 전환)에 체크되어 있는지 확인하세요.
- 이미 메인 페이지에 이 캘린더를 임베드 링크로 걸어두신 걸 보면 아마 공개 설정이 되어있을
  가능성이 높지만, 혹시 "액세스 거부" 오류가 뜨면 이 설정부터 확인해주세요.

## 4. 참고
- 캘린더 조회가 실패하거나 키가 없으면, 화면에 "캘린더에서 직접 확인하기" 링크가 대신 떠서
  원래 구글 캘린더 페이지로 이동할 수 있게 해뒀습니다.
- 시간이 있는 일정은 "16:00–17:00"처럼, 종일 일정은 "종일"로 표시됩니다.
