# 사이트 로그인(구글 계정 인증) 설정 방법

모든 페이지는 이제 `auth.js`를 통해 "학교 구글 계정으로 로그인해야만" 내용이 보이도록 되어 있습니다.
작동하려면 딱 한 가지, Google Cloud Console에서 **OAuth 클라이언트 ID**를 하나 발급받아야 합니다.
(무료입니다. 5분 정도 걸려요.)

## 1. 클라이언트 ID 발급
1. https://console.cloud.google.com 접속 (학교 구글 계정으로 로그인해도 되고, 개인 계정도 상관없습니다)
2. 상단의 프로젝트 선택 → **새 프로젝트** 만들기 (이름은 자유롭게, 예: "경성고 교무도구")
3. 왼쪽 메뉴 **API 및 서비스 → OAuth 동의 화면**
   - User Type: **외부(External)** 선택 (교육청 Workspace 관리자 권한이 없다면 이걸로 충분합니다)
   - 앱 이름, 이메일 등 필수 항목만 채우고 저장 (게시 상태는 "테스트" 그대로 두어도 실제 사용에는 문제없습니다.
     다만 "테스트" 상태에서는 테스트 사용자로 등록된 계정만 로그인되니, 아래 4번처럼 필요한 선생님 이메일을
     테스트 사용자로 추가해주세요. 전체 공개로 바꾸려면 구글 심사가 필요할 수 있어 학교 내부용으로는
     테스트 사용자 등록 방식을 추천합니다.)
4. **OAuth 동의 화면 → 테스트 사용자**에서 로그인이 필요한 선생님들의 학교 이메일을 추가
   (테스트 사용자로 등록된 계정만 로그인 화면이 정상적으로 뜹니다)
5. 왼쪽 메뉴 **API 및 서비스 → 사용자 인증 정보 → 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
   - 애플리케이션 유형: **웹 애플리케이션**
   - 이름: 자유롭게 (예: "교무도구 사이트")
   - **승인된 자바스크립트 원본**에 사이트 주소 추가:
     `https://jocular-panda-50501a.netlify.app`
     (다른 도메인을 새로 쓰게 되면 그 주소도 추가해야 합니다)
   - 만들기 클릭 → **클라이언트 ID** 값이 발급됩니다 (`~~~.apps.googleusercontent.com` 형태)

## 2. 사이트에 적용
`auth.js` 파일 맨 위쪽의 아래 줄을 발급받은 값으로 교체하세요.

```javascript
const GOOGLE_CLIENT_ID = 'YOUR_CLIENT_ID.apps.googleusercontent.com'; // ← 여기를 교체
const ALLOWED_DOMAIN = 'senedu.kr'; // 학교 구글 계정 도메인이 다르면 여기도 수정
```

바로 아래 줄의 `ALLOWED_DOMAIN`이 실제 선생님들 이메일 도메인과 정확히 일치하는지도 한 번 더
확인해주세요 — 다르면 아무도 로그인이 안 됩니다.

## 3. 참고
- 로그인 정보는 브라우저의 sessionStorage에 12시간 저장되며, 탭/브라우저를 닫으면 사라집니다.
- 화면 오른쪽 위에 로그인한 이름과 "로그아웃" 버튼이 항상 표시됩니다.
- 학교 도메인이 아닌 계정으로 로그인을 시도하면 오류 메시지가 뜨고 내용은 보이지 않습니다.
- 교실 사용 신청 폼의 Apps Script 연동은 `room-request-setup.md` 파일을 참고하세요.
